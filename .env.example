# =============================================================================
# ALESQUI INTELLIGENCE - DEPLOYMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and fill in your values
# =============================================================================

# =============================================================================
# COMPANY INFORMATION
# =============================================================================
COMPANY_NAME=My Company Inc.

# =============================================================================
# INITIAL ADMIN USER (Optional)
# =============================================================================
# On first startup, if no users exist, an admin account will be created automatically.
# This is useful for corporate deployments to have an admin ready immediately.
#
# Customize the admin email address (optional, defaults to admin@company.com)
# INITIAL_ADMIN_EMAIL=admin@mycompany.com
#
# Set a specific password (optional, leave commented to auto-generate)
# If not set, a secure random password will be generated and displayed in the logs
# INITIAL_ADMIN_PASSWORD=
#
# To see the generated password, check the backend logs:
#   docker-compose logs backend | grep InitialAdmin
#
# 🔒 IMPORTANT: Change the password after first login!

# =============================================================================
# MONGODB CONFIGURATION
# Choose ONE of the following options:
# =============================================================================

# --- OPTION 1: Local MongoDB (Docker container) ---
# Uncomment these lines to use local MongoDB
# Then start with: docker-compose --profile local-db up -d
MONGODB_ROOT_USER=admin
MONGODB_ROOT_PASSWORD=CHANGE_THIS_SECURE_PASSWORD
MONGODB_DATABASE=alesqui_intelligence
# Connection string for local MongoDB
MONGODB_URI=mongodb://admin:CHANGE_THIS_SECURE_PASSWORD@mongodb:27017/alesqui_intelligence?authSource=admin

# --- OPTION 2: MongoDB Atlas (Cloud) ---
# Uncomment this line and comment out the local MongoDB variables above
# Then start with: docker-compose up -d
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/alesqui_intelligence?retryWrites=true&w=majority

# =============================================================================
# SECURITY - JWT
# =============================================================================
# Generate a secure secret (minimum 256 bits / 32 characters)
# Use: ./scripts/generate-secrets.sh
# Or: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_JWT_SECRET_TO_A_RANDOM_SECURE_ONE
JWT_EXPIRATION=900000
JWT_REFRESH_EXPIRATION=604800000

# =============================================================================
# AI CONFIGURATION - OpenAI
# =============================================================================
# Get your API key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# =============================================================================
# URLS AND CORS CONFIGURATION
# =============================================================================
# URL where your users will access the frontend
# Examples:
#   - http://localhost (local development)
#   - https://intelligence.mycompany.com (production)
FRONTEND_URL=http://localhost

# Backend API URL
# For Docker deployment, this is usually http://localhost:8080
VITE_API_BASE_URL=http://localhost:8080

# Additional allowed origins for CORS (comma-separated)
# Example: https://app-test.mycompany.com,https://app-dev.mycompany.com
CORS_ADDITIONAL_ORIGINS=

# =============================================================================
# DOCKER CONFIGURATION
# =============================================================================
# Docker images to use from Docker Hub
BACKEND_IMAGE=alesquiintelligence/backend:latest
FRONTEND_IMAGE=alesquiintelligence/frontend:latest

# Ports to expose (change if 80/8080 are already in use)
FRONTEND_PORT=80
BACKEND_PORT=8080

# Java Options (customize memory settings if needed)
JAVA_OPTS=-XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0
